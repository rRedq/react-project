export {
  getCards,
  getCountries,
  getAnimation,
} from './model/selectors/cardsSelectors';

export { addCard, changeAnimateStatus } from './model/slice/cardsSlice';

export { cardsReducer } from './model/slice/cardsSlice';
